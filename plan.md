# 버전 복구 UI 고도화 및 버그 수정 계획

버전 기록 확인 시 드롭다운이 닫히는 버그를 수정하고, 사용자가 요청한 "프리미엄 버전 복구 모달"을 구현합니다.

## 1. 접근 방식
1.  **버그 수정**: `showVersionHistory` 호출 시 드롭다운이 닫히지 않도록 이벤트 전파를 제어하고, `prompt` 대신 커스텀 모달을 사용합니다.
2.  **모달 UI 설계**: 좌측 리스트와 우측 미리보기가 결합된 2컬럼 레이아웃의 모달 창을 구현합니다.
3.  **미리보기 강화**: 버전을 선택할 때마다 우측 영역에 해당 버전의 내용을 즉각 렌더링하여 확인할 수 있게 합니다.

---

## 2. 세부 구현 단계

### 단계 1: 모달 HTML 구조 추가
*   **파일**: `index.html`
*   **내용**: `#history-modal` ID를 가진 모달 컨테이너와 내부 구조(리스트 영역, 미리보기 영역, 버튼군) 추가.

### 단계 2: 모달 스타일링 (Premium CSS)
*   **파일**: `src/styles/editor.css`
*   **내용**: 
    *   배경 블러(Backdrop-filter), 둥근 모서리, 부드러운 트랜지션 적용.
    *   버전 리스트 아이템의 호버 및 활성화 상태 스타일 정의.
    *   미리보기 영역의 스크롤 및 텍스트 스타일 정의.

### 단계 3: 모달 연동 로직 구현 (JS)
* 1.  **에디터 기능 고도화 (`src/editor.js`, `src/main.js`)**:
    *   볼드/이탤릭 토글 로직을 스마트하게 변경 (주변 문맥 파악).
    *   커스텀 Undo/Redo 매니저 구현 및 키 바인딩.
    *   **[NEW]** 실시간 자동 저장(Auto-save) 로직 추가.
2.  **UI/UX 보강 (`index.html`, `src/styles/editor.css`)**:
    *   버튼 툴팁 텍스트를 사용자가 이해하기 쉬운 명칭으로 통일.
    *   헤더 바의 높이를 고정하여 좌우 패널의 시각적 균형 유지.
3.  **데이터 무결성 및 버전 분리 (`src/storage.js`, `src/main.js`)**:
    *   문서별 버전 기록이 섞이지 않도록 ID 기반 필터링 강화.
    *   복원 시 대상 문서의 ID를 명확히 지정하여 타 문서 덮어쓰기 방지.및 모달 닫기.
    *   닫기/외부 클릭 이벤트: 모달 종료.

---

## 3. 고려 사항
*   **성능**: 히스토리가 많을 경우를 대비해 리스트 영역에만 스크롤 적용.
*   **데이터 보호**: 복원 전 현재 상태를 새로운 버전으로 자동 저장하는 기존 안전장치 유지.

---

## 4. 체크리스트 (구현 단계)

- [x] 단계 1: 모달 HTML 레이아웃 추가 (`index.html`)
- [x] 단계 1: 볼드/이탤릭 토글 로직 개선
- [x] 단계 2: Undo/Redo 기능 구현 및 연동
- [x] 단계 3: 버튼 툴팁 상세화
- [x] 단계 4: 패널 헤더 높이 일원화 (CSS 수정)
- [x] 단계 5: 실시간 자동 저장(Auto-save) 적용
- [x] 단계 6: 문서별 버전 격리 및 복원 로직 버그 수정최종 테스트